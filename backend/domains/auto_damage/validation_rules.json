{
  "required_fields": [
    "policy_number",
    "applicant_name",
    "loss_date",
    "vehicle_make",
    "vehicle_model",
    "vehicle_year",
    "loss_description"
  ],
  "field_validations": {
    "policy_number": {"pattern": "^[A-Z]{2}\\d{8}$", "format_example": "AB12345678"},
    "vin": {"pattern": "^[A-HJ-NPR-Z0-9]{17}$", "required": false},
    "vehicle_year": {"min": 1990, "max": 2027},
    "estimated_repair_amount": {"min": 0, "max": 150000},
    "loss_date": {"max_days_past": 90}
  },
  "cross_reference_checks": [
    {
      "name": "policy_active_on_loss_date",
      "fields": ["loss_date", "policy_expiry"],
      "description": "Loss date must be within policy active period"
    },
    {
      "name": "vin_format_valid",
      "fields": ["vin"],
      "description": "VIN must be 17 characters, no I/O/Q"
    }
  ]
}
